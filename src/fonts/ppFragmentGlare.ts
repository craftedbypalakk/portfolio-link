import localFont from "next/font/local";

/** PP Fragment Glare Regular — Figma hero title (node 143:173019). Personal-use (Pangram Pangram). */
export const ppFragmentGlare = localFont({
  src: [{ path: "./pp-fragment-glare/PPFragment-GlareRegular.otf", weight: "400", style: "normal" }],
  variable: "--font-pp-fragment-glare",
  display: "swap",
});
